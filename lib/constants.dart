import 'package:flutter/material.dart';

const mainBackgroundColor =  Color(0xFFeaeaea);
const fontStyleMenuItem = TextStyle(
    fontSize: 18,
    fontWeight: FontWeight.w600,
);
const homeGradientButton = LinearGradient(
  begin: Alignment.topRight,
  end: Alignment.bottomLeft,
  colors: [
    Color(0xFF0093E0),
    Color(0xFF80D0C7),
  ],
);

const profilGradientButton = LinearGradient(
  begin: Alignment.topRight,
  end: Alignment.bottomLeft,
  colors: [
    Color(0xFFFBAB7E),
    Color(0xFFF7CE68),
  ],
);

const settingsGradientButton = LinearGradient(
  begin: Alignment.topRight,
  end: Alignment.bottomLeft,
  colors: [
    Color(0xFF08AEEA),
    Color(0xFF2AF598),
  ],
);

const logoutGradientButton = LinearGradient(
  begin: Alignment.topRight,
  end: Alignment.bottomLeft,
  colors: [
    Color(0xFFFFFF),
    Color(0xFFFFFF),
  ],
);